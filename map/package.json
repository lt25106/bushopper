{
  "name": "map",
  "version": "0.1.0",
  "main": "deploy/j.js",
  "scripts": {
    "build": "esbuild t.ts --bundle --minify --format=esm --target=esnext --outfile=deploy/j.js",
    "watch": "esbuild t.ts --bundle --minify --format=esm --target=esnext --outfile=deploy/j.js --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lt25106/bushopper.git"
  },
  "author": "lt25106",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/lt25106/bushopper/issues"
  },
  "homepage": "https://github.com/lt25106/bushopper#readme",
  "description": "",
  "dependencies": {
    "leaflet": "^1.9.4",
    "leaflet-geometryutil": "^0.10.3"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.20"
  }
}
